export const environment = {
  production: true,
  apiUrl: 'https://api.example.com',
  googleApiKey: '', // Should be configured securely, not hardcoded
  googleClientId: '' // Should be configured securely, not hardcoded
};